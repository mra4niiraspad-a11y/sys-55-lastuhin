LOGFILE="/home/last/backup.log"

SOURCE="/home/last/"

DESTINATION="/tmp/backup/"



rsync -av \

  --exclude=".*" \

  --exclude="daily_backup.sh" \

  --exclude="backup.log" \

  --checksum \

  "$SOURCE" "$DESTINATION" >> "$LOGFILE" 2>&1




if [ $? -eq 0 ]; then

    echo "$(date '+%Y-%m-%d %H:%M:%S'): Backup completed successfully." >> "$LO>

else

    echo "$(date '+%Y-%m-%d %H:%M:%S'): Backup failed!" >> "$LOGFILE"

